## 2026-05-28 - Interactive Element Accessibility
**Learning:** Icon-only buttons over dynamic backgrounds (like AR feeds) require explicit focus rings with contrasting offsets and precise ARIA labels to be both perceivable and understandable by screen readers.
**Action:** Ensure all icon-only interactions include `aria-label` and high-contrast `focus-visible:ring-*` classes.
